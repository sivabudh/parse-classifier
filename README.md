To see something, just do the following:

- Make sure you have python 3+ installed. I used 3.5.2.
- Then, `pip install -r requirements.txt`
- Then, `python show_vlan.py` or `python parse_classifier.py`
- Rejoice!
